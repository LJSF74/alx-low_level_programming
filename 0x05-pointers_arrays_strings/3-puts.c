#include "main."
#include <stdio.h>
/**
 * _puts - prints string in var
 * @str: string
 * Return: void
 */
void _puts(char *str)
{
	int i = 0;

	while (*(str + i) != '\0')
	{
		printf("%c", *(str + i);
	}
}
